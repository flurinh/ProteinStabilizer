# Exact core versions used for the 2026-07-16 ESM-C 6B caches and checkpoints.
# The Hugging Face 6B adapter does not import `esm`; that incompatible package
# is intentionally confined to the separate 600M environment.
biopython==1.86
h5py==3.16.0
joblib==1.5.3
numpy==2.5.1
openpyxl==3.1.5
pandas==3.0.3
pyarrow==23.0.1
# Match the runtime used to fit the portable ExtraTrees accuracy prior.
scikit-learn==1.8.0
scipy==1.18.0
torch==2.9.1
transformers @ git+https://github.com/Biohub/transformers.git@ef32577f55da19a4989cd7b22e004dc43a4998cb
